# Scope, Bugs & TODO

---

## Deferred

The following features are explicitly **out of scope for now** and tracked here to prevent accidental implementation.

> ⚠️ **Radial (Rotary) mode is NOT YET IMPLEMENTED.**
> `AxialMotorConfig` exists as a design stub — it has no geometry generation, no radial coil patterns, and no torque sweep. The Linear/Radial toggle is **disabled** in the UI and clearly labelled `TODO / not-yet-implemented`. Do NOT attempt to implement radial geometry.

---

## Known Issues (Active Bugs)

This is the consolidated, high-level bug tracker. **All currently ACTIVE bugs are
tracked in the round sections below**

| #   | Bug                                                            | Status   | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | -------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `validate_write_preconditions` always reports 12 layers        | RESOLVED | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2   | Back iron graphics don't clear when toggling off               | RESOLVED | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 3   | Magnet preview bars are offset from the three-phase slot zones | RESOLVED | Anchor each pitch cell's right edge (solid magnet + trailing gap) on the pattern's B-phase slot centres (A1 +, B1 neutral, C1/A2 −, ...) using `routing_dimensions.pole_regions`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 4   | Mover geometry was inconsistent with the routed slot bands     | RESOLVED | Magnet mover sizing now follows the coreless mover-array spec: `pole_pitch τ_p = P_e / 2` (one electrical cycle contains two alternating poles), `W_m = τ_p × k_fill`, `W_gap = τ_p − W_m`, `T_m = 0.5 × τ_p`. The user inputs magnet X Length in mm (clamped to `[0.5, 1.0] × τ_p`); `k_fill = W_m / τ_p` is DERIVED and displayed (warning above 0.85 for flux leakage, 1.00 = end-to-end magnets). Defaults: slot/electric pitch 12 mm → τ_p 6 mm, 12 poles, W_m 4.5 mm, gap 1.5 mm, T_m 3.0 mm, coil span 72 mm, active area 147 mm with 75 mm travel. The preview magnet strip is anchored slot-centre-to-slot-centre from the routing crate's `pole_regions` (slot start/end are never inferred from the magnet config). |
| 5   | Position slider bounds were geometric, not physical            | RESOLVED | Slider min/max are now the FIRST/LAST STABLE EQUILIBRIUM rest positions of the mover array centre under baseline currents (`I_A=+I, I_B=0, I_C=−I`). New `pcbmotorgen_simulation::equilibrium` module: Clarke transform → θe = π/6 → N-pole field peak at `P_e/12`; rest phase `φ = (P_e/12 + ((N−1)/2)·τ_p) mod P_e`; envelope `min = φ`, `max = φ + P_e·floor((A + τ_p/2 − x_peak − L_array/2 − φ)/P_e)`. Exposed via the `travel_envelope` command (`TravelEnvelopeIpc`) and mirrored by `mockTravelEnvelope` in browser dev mode. Reference vectors verified in Rust + vitest: N=4/P_e=12 mm → centre rests at 10 mm with leading edge at −2 mm; defaults (N=12, A=147 mm) → Range 10–106 mm. MotionStore falls back to the old geometric bounds until an envelope arrives. |
| 6   | Coil preview and design reflection dimensions disagreed; "Active area length" mislabeled the trace span | RESOLVED | The routed traces span the routing domain (active area + 2 × end padding), but the reflection drew the PCB at the active-area length only. Previews initially trusted the CONFIGURED domain — wrong: the infinity braid floors whole periods after reserving interleave slack, so the REAL traces measure **203.6 mm** against a 207 mm nominal domain (pinned by `crates/pcbmotorgen-routing/tests/braid_trace_span.rs`). All consumers now MEASURE the returned payload (`measureTrace` in previewGeometry): DesignDimensions shows the measured "PCB trace total (X)" ("meas." tag), the reflection header shows `traces = <measured> mm`, and the iso view draws the PCB across the measured span in the ROUTING frame with the copper active region dashed-in plus a measured-span dimension line. The mover strip everywhere shares ONE anchor (`stripBoundsDomainMm`: pole-region slot-centre rest bounds + motion offset) consumed by the canvas overlay, iso view and BOTH MoverPositionControls instances — whose readout now prints the drawn "On PCB" extent and travel-used/total instead of mismatched active-frame numbers, so slider, picture and numbers can no longer disagree. ISO_Z_EXAG is single-sourced in lib/geometry. An amber note explains configured-vs-measured drift when > 0.5 mm. The IPC contract (`active_area_length_m`) is unchanged. |
| 7   | Maximum slider position ignored the real track; mover could settle past energized coils | RESOLVED | The travel envelope used the CONFIGURED ACTIVE-AREA length with a mirrored overhang rule (defaults: 10–106 mm), so it neither knew about the braid's sub-period slack nor required the array to stay fully over track — and its positions lived in a different frame than every drawing. New `travel_envelope_on_track(P_e, N, track_length)` in `pcbmotorgen_simulation::equilibrium`: same lattice (`x ≡ φ (mod P_e)`, φ = 10 mm for P_e = 12 mm), but positions are ABSOLUTE TRACK coordinates under an EDGE rule — min = smallest centre with leading edge ≥ 0, max = largest with trailing edge ≤ track end. The `travel_envelope` command now MEASURES the routed segment X-extent (regenerating the pattern; falls back to the configured domain on routing failure). Product-reference derivation pinned in Rust: N=4, P_e=12 mm, measured track 203.6 mm → envelope **22–190 mm**, i.e. max array centre 190 mm with trailing edge at 202 mm (next lattice point would overhang to 214 mm); robust to 204 mm period-grid rounding. Frontend unified: MotionStore positions/bounds are track-frame; strip bounds are CENTRE-based everywhere (`stripBoundsDomainMm(config, motion)` = position ± coil_span/2) consumed by canvas overlay, iso view and readouts — "On PCB" numbers equal drawn edges exactly; geometric fallback is padding-shifted; mock envelope mirrors the same formula (mock defaults now 46–166 mm, verified live: max readout `On PCB: 130 - 202`, travel 120/120). HoldingForceChart zeros sit at φ + k·P_e in the same frame. FIXUP: first cut of the measured-track path returned the routing crate's MILLIMETRE span as metres → real-mode slider hit ~203 554 mm and the CoilPreview camera fit shrank the board to a speck; extraction into `routed_track_length_m()` with mm→m conversion is pinned by 3 Rust unit tests, and MotionStore now sanity-gates fetched envelopes (finite, ordered, within the routing domain) so a backend unit slip degrades to the geometric fallback instead of breaking the UI. FIXUP 5 (final, user-specified "keep it simple"): endpoints are COIL-CAPTURE positions that scale with the ELECTRICAL PERIOD only (independent of N) — `min = padding + (2/3)·P_e` (first coil captures the first pole at electrical 240°, Phase A@120/B@0/C@240) and `max = (padding + active) − (3/4)·P_e` (last coil at the 270° complement). Defaults → **38 … 168 mm**. `travel_envelope_over_slots(P_e, N, slot_start, slot_end)`; `rest_phase_m` stays track-frame `(slot_start + φ) mod P_e` for chart zeros (N-independent endpoints; N-dependent phase marker). `routed_track_length_m` removed. Pins: Rust `twelve_pole_defaults_pins_38_to_168_mm` / `four_pole_shares_the_capture_endpoints` / `six_pole_shares_endpoints_but_has_its_own_phase` / `endpoints_scale_with_electrical_period` / `narrow_slot_region_clamps_max_to_min`; TS mock + envelope-chain assert 0.038/0.168 m, bounds 38/168, strip edges 2–74 / 132–204. |

---

## Wishes (Deferred / Backlog)

Improvements the user has requested but deferred to a future round.

| #   | Wish                | Effort                  | Notes                                                                                                                                                                                                                                                                      |
| --- | ------------------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| W1  | Backplane under PCB | Medium (future feature) | A back-iron-like plate sits BELOW the PCB (below the bottom copper layer, below the coils) to increase the field for symmetry. NOTE: the "above the magnets" back iron and the magnet-arrangement options were REMOVED from the product scope (2026-08) — the mover is always the plain alternating array. A future backplane would need a new config field back in `SimulationInput`. |
| W2  | Real per-phase holding-force amplitudes in the position chart | Medium | `HoldingForceChart` currently draws NORMALIZED per-phase waves (`−sin(2π(x − φ − p·P_e/N)/P_e)`, three waves at 120° for a 3-phase motor). Replace with sampled per-phase thrust from the simulation crate's `ForceEvaluator` across `[min, max]` so amplitudes are physical (N), and mark detent/trough positions. |

---
